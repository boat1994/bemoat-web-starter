'use client'

export function BlogAiWorkflow() {
  return (
    <div style={{ fontSize: 12, opacity: 0.8 }}>
      Blog AI workflow placeholder. The schema keeps the extension point from Bogus dev branch.
    </div>
  )
}
