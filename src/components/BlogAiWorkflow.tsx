'use client'

export function BlogAiWorkflow() {
  return (
    <div style={{ fontSize: 12, opacity: 0.8 }}>
      Blog AI workflow placeholder. The schema keeps a reusable extension point for project-specific automation.
    </div>
  )
}
